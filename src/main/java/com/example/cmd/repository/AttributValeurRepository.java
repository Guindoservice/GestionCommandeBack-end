package com.example.cmd.repository;

import com.example.cmd.model.AttributValeur;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AttributValeurRepository extends JpaRepository<AttributValeur,Long> {
}
